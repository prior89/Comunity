# 순수 Python 패키지만 (Rust 완전 제거)
fastapi==0.95.2
uvicorn==0.22.0
pydantic==1.10.12

# AI (순수 Python만)
openai==0.28.0
requests==2.31.0

# 기본 라이브러리
aiohttp==3.8.5
feedparser==6.0.10
python-dotenv==1.0.0

# MongoDB (순수 Python)
motor==3.2.0
pymongo==4.5.0

# 필수
tzdata==2023.3