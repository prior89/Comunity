# 휠 버전으로 고정 (Rust 빌드 방지)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2

# AI 라이브러리 (휠 제공 버전)
groq==0.12.0
openai==1.54.5

# 기본 라이브러리 (휠 제공)
aiohttp==3.10.10
feedparser==6.0.11
python-dotenv==1.0.1
pydantic-settings==2.6.1

# MongoDB (휠 제공)
motor==3.6.0
pymongo==4.10.1

# 타임존
tzdata>=2023.3