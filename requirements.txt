# Rust 불필요한 버전으로 고정
fastapi==0.104.1
uvicorn[standard]==0.24.0  
pydantic==1.10.18

# AI 라이브러리 (순수 Python 휠)
groq==0.8.0
openai==1.3.7

# 기본 라이브러리 (순수 Python)
aiohttp==3.8.6
feedparser==6.0.10
python-dotenv==1.0.0

# MongoDB (순수 Python)
motor==3.3.2
pymongo==4.6.0

# 타임존
tzdata>=2023.3