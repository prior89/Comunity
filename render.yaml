services:
  - type: web
    name: ai-news-platform
    env: python
    plan: starter
    buildCommand: python -m pip install --upgrade pip setuptools wheel && python -m pip install -r news/requirements-render.txt
    startCommand: cd news && python main.py
    autoDeploy: true
    envVarGroups:
      - ai-news-secrets